#include<cstdio>
#include<cmath>
#include<cstring>
#include<time.h>
#include<vector>
#include<queue>
#include<list>
#include<stack>
#include<set>
#include<map>
#include<algorithm>
#include<iostream>
using namespace std;

/*int main(){
	const int N = 10008, K = 208;
	int n, k, a, b, x[N], g[K], d[K][N];
	while( scanf("%d%d%d%d",&n,&k,&a,&b)==4 ){
		int L = 0;
		for(int i=0; i<n; i++){ scanf("%d",&x[i]); L += x[i]; }
		L /= n;

		for(int i=0; i<k; i++) scanf("%d",&g[i]);
		if( n<a || n>k*b ){ printf("solution.\n"); continue; }
		memset( d, -1, sizeof(d) );
		d[0][0] = 0;
		for(int r=1; r<=k; r++){
			d
		}
	}
	return 0;
}*/

